# 论文目录与章节要点

> 更新时间：2026-05-26
> 目标：先搭建毕业论文骨架，明确每章要回答的问题、可引用的项目文档和后续需要补充的实验材料。本文档不是最终论文正文，而是论文写作的结构底稿。

## 论文暂定题目

可选题目：

1. 基于教师画像与遗传算法的高校智能排课系统设计与实现
2. 融合 LightGBM 片段评分与遗传算法的高校排课优化方法研究
3. 面向教师偏好的智能排课系统设计与反馈训练机制研究

推荐题目：

```text
基于教师画像与 LightGBM-GA 混合优化的高校智能排课系统设计与实现
```

理由：

- 突出教师画像创新点。
- 突出 LightGBM + GA 的算法组合。
- 兼顾系统设计与工程实现。
- 适合毕业设计论文表达。

## 摘要要点

摘要后续可以围绕以下内容展开：

- 高校排课存在多约束、多目标、人工调整成本高的问题。
- 单纯规则系统难以学习教师隐性偏好，直接使用大模型生成课表又难以保证硬约束合法性。
- 本文设计一种基于教师画像、LightGBM 片段评分和遗传算法全局优化的智能排课系统。
- 系统将教师自然语言需求结构化为画像特征，并构造片段级训练样本。
- LightGBM 用于候选排课片段质量评分，GA 用于完整课表全局搜索。
- 通过真实课表、AI 生成满意课表和人工反馈构建训练数据，并设计消融实验验证模型和教师画像的贡献。

关键词建议：

```text
智能排课；教师画像；遗传算法；LightGBM；反馈学习；消融实验
```

## 第 1 章 绪论

### 1.1 研究背景

要点：

- 高校排课涉及课程、教师、班级、教室、时间片等多类资源。
- 排课问题具有明显的组合优化特征。
- 传统人工排课工作量大，容易产生冲突，调整成本高。
- 教师偏好、课程分布、教室类型等软约束难以通过简单规则完全表达。

### 1.2 研究意义

要点：

- 降低教务人员排课和调整成本。
- 提高课表冲突检测和方案生成效率。
- 通过教师画像提升排课方案的人性化程度。
- 通过反馈训练让系统逐步学习真实排课偏好。

### 1.3 国内外研究现状

可写方向：

- 规则驱动排课方法。
- 遗传算法、模拟退火、禁忌搜索等启发式优化方法。
- 机器学习在教育管理和排课辅助中的应用。
- 大模型在自然语言偏好解析中的应用边界。

### 1.4 本文主要工作

建议写成 4 点：

1. 设计教师画像建模方法，将自然语言偏好转为结构化约束和特征。
2. 设计片段级训练样本构建链路，融合真实课表、AI 满意课表和人工反馈。
3. 设计 LightGBM-GA 混合优化排课方法，使用模型局部评分引导 GA 全局搜索。
4. 设计多组消融实验，验证模型评分器和教师画像增强的实际贡献。

### 1.5 论文结构安排

简述各章内容。

对应文档：

- `docs/architecture/11-核心链路论文版总设计.md`
- `docs/roadmap/02-理论与实验设计推进路线.md`

## 第 2 章 相关技术与理论基础

### 2.1 高校排课问题建模

要点：

- 排课问题属于多约束组合优化问题。
- 资源包括教师、班级、教室、时间片。
- 约束分为硬约束和软约束。

### 2.2 遗传算法

要点：

- 遗传算法基本流程：编码、初始化、选择、交叉、变异、适应度评价。
- 适合解决大规模组合优化问题。
- 在排课场景中可用于完整方案搜索。

### 2.3 LightGBM 模型

要点：

- LightGBM 是基于 GBDT 的高效梯度提升树模型。
- 适合结构化表格数据。
- 可用于候选片段质量评分。

### 2.4 RandomForest 与对照模型

要点：

- RandomForest 是 bagging 思路的集成模型。
- 可作为 LightGBM 的合理对照组。
- XGBoost 可作为可选扩展对照。

### 2.5 sigmoid 评分归一化

要点：

- 用于将模型分、规则惩罚分统一映射到 `[0, 1]`。
- 避免极端分数主导 GA 搜索。

对应文档：

- `docs/architecture/09-GA编码与适应度函数设计.md`
- `docs/architecture/10-LightGBM选型与模型对比说明.md`

## 第 3 章 系统需求分析与总体设计

### 3.1 需求分析

功能需求：

- 教学任务管理。
- 教师画像录入与解析。
- 排课方案生成。
- 多方案展示。
- 冲突检测。
- 人工调整与反馈记录。

非功能需求：

- 可解释性。
- 可扩展性。
- 可复现性。
- 生成效率。

### 3.2 系统总体架构

主链路：

```text
基础数据 → 教师画像 → 候选片段 → LightGBM评分 → GA优化 → 方案输出 → 人工反馈 → 模型训练
```

### 3.3 模块划分

模块：

- 前端交互模块。
- Java 业务编排模块。
- Python 排课生成模块。
- 教师画像模块。
- 模型训练与推理模块。
- 反馈样本采集模块。

### 3.4 数据流设计

重点讲：

- 教师画像快照。
- 真实课表导入。
- 片段级样本。
- 生成方案与反馈事件。

对应文档：

- `docs/architecture/01-排课架构设计.md`
- `docs/architecture/11-核心链路论文版总设计.md`

## 第 4 章 教师画像建模与训练数据构建

### 4.1 教师画像设计

要点：

- 基础画像。
- 时间画像。
- 负载画像。
- 空间画像。
- 行为画像。

### 4.2 教师自然语言偏好结构化

要点：

- 自然语言只是输入体验。
- 结构化 JSON 才是排课事实。
- 区分硬不可排和软偏好。

### 4.3 真实课表数据采集

要点：

- 学校真实课表作为高可信正样本。
- 需要字段标准化、周次转换、节次转换、名称映射。

### 4.4 AI 生成满意课表扩充

要点：

- AI 生成课表必须经过人工筛选。
- 4~5 分方案作为正样本。
- 不满意片段作为负样本或待分析样本。

### 4.5 片段级样本构建

主样本单位：

```text
teaching_task + teacher + class_group + time_slot + classroom + context
```

### 4.6 标签与权重设计

要点：

- 真实课表片段为高可信正样本。
- 人工调整后为强正样本。
- 人工调整前为负样本。
- 规则生成 sample.csv 只作为冷启动弱监督样本。

对应文档：

- `docs/architecture/02-教师画像作用路径设计.md`
- `docs/architecture/05-模型训练数据链路设计.md`
- `docs/architecture/06-真实课表导入字段模板.md`
- `docs/architecture/07-AI生成课表人工筛选标准.md`

## 第 5 章 LightGBM-GA 混合排课算法设计

### 5.1 算法总体流程

流程：

```text
候选空间构建 → 片段质量评分 → GA初始化 → 进化搜索 → repair修复 → 多方案输出
```

### 5.2 GA 编码设计

要点：

- 染色体表示完整课表。
- 基因表示一个教学任务安排。
- 基因包含模板集、时间槽和教室。

### 5.3 约束建模

硬约束：教师、班级、教室、容量、时间范围、教师硬不可排。  
软约束：教师偏好、早晚课、连续授课、班级负载、教室匹配。

### 5.4 适应度函数设计

公式：

```text
Fitness = HardPenalty × W_h
        + SoftPenalty × W_s
        + TemplatePenalty × W_t
        - ModelQuality × W_m
```

### 5.5 LightGBM 候选片段评分

要点：

- 输入为片段级结构化特征。
- 输出 `[0, 1]` 质量分。
- 不覆盖硬约束。

### 5.6 sigmoid 归一化

公式：

```text
raw_i = model_i - λ × penalty_i
q_i = sigmoid(raw_i / T)
```

### 5.7 遗传操作与 repair

要点：

- 初始化。
- 选择。
- 交叉。
- 变异。
- 冲突修复。

对应文档：

- `docs/architecture/09-GA编码与适应度函数设计.md`
- `docs/architecture/10-LightGBM选型与模型对比说明.md`

## 第 6 章 系统实现

### 6.1 前端实现

可写：

- 生成配置。
- 多方案展示。
- 冲突结果展示。
- 教师画像录入。

### 6.2 Java 后端实现

可写：

- 排课任务管理。
- ML 调用。
- SSE 监听。
- 方案解析入库。
- 反馈事件导出。

### 6.3 Python 排课服务实现

可写：

- 数据读取。
- 模板枚举。
- GA 生成。
- LightGBM 推理。
- schemes.json 输出。

### 6.4 数据存储设计

可写：

- 教学任务表。
- 教师画像表。
- 排课方案表。
- 排课项表。
- 反馈事件表。

对应文档：

- `docs/implementation/01-GA排课生成链路实现说明.md`
- `docs/implementation/02-教师画像JSONL快照接入说明.md`
- `docs/implementation/03-排课链路验证与排障说明.md`

## 第 7 章 实验设计与结果分析

### 7.1 实验数据来源

要点：

- 学校真实课表。
- AI 生成满意课表。
- 人工调整反馈。
- 规则冷启动样本。

### 7.2 实验设置

固定：

- 同一批教学任务。
- 同一套硬约束。
- 同一训练 / 验证 / 测试划分。
- 同一 GA 参数。

### 7.3 消融实验组

核心组：

1. 规则 + GA。
2. GA + RandomForest。
3. GA + LightGBM。
4. GA + LightGBM + 教师画像增强。

### 7.4 评价指标

模型指标：AUC、LogLoss、Precision@K、NDCG@K。  
业务指标：冲突数、教师偏好满足率、人工调整次数、平均 fitness、生成耗时、方案采纳率。

### 7.5 实验结果分析

后续待补：

- 模型对比结果表。
- 业务指标对比图。
- 教师画像增强前后差异。
- sigmoid 归一化稳定性分析。

对应文档：

- `docs/architecture/08-模型消融实验设计方案.md`

## 第 8 章 总结与展望

### 8.1 工作总结

总结：

- 完成智能排课系统链路设计。
- 完成教师画像建模。
- 完成 LightGBM-GA 混合优化设计。
- 完成训练数据构建方案。
- 完成消融实验设计。

### 8.2 不足

可写：

- 真实课表和人工反馈数据量仍有限。
- 教师画像质量依赖结构化解析准确性。
- 模型评分仍是局部评分，无法完全代表整张课表质量。
- 实验结果需要更多真实场景验证。

### 8.3 展望

可写：

- 引入更多学期真实数据。
- 增强 pairwise ranking 偏好学习。
- 优化 GA 参数自适应调整。
- 增加更细粒度的教师画像和教务反馈标注。

## 当前暂缺材料

后续需要补：

1. 学校真实课表示例数据。
2. AI 生成课表人工筛选样本。
3. 不同模型训练结果。
4. 消融实验业务指标结果。
5. 系统关键页面截图。
6. 核心链路图、训练链路图、算法流程图。

## 写作优先级

建议顺序：

1. 先写第 3 章系统总体设计。
2. 再写第 5 章 LightGBM-GA 算法设计。
3. 接着写第 4 章教师画像与训练数据。
4. 然后写第 6 章系统实现。
5. 实验数据出来后写第 7 章。
6. 最后写摘要、第 1 章和第 8 章。

不要从摘要开始写，摘要应该最后再定。
